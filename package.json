{
  "scripts": {
    "start": "npm-run-all --parallel serve proxy",
    "serve": "serve -l 5000",
    "proxy": "local-ssl-proxy --source 8443 --target 5000",
    "ci": "npm ci"
  },
  "dependencies": {
    "mapbox-gl": "^3.16.0"
  },
  "devDependencies": {
    "local-ssl-proxy": "^2.0.5",
    "npm-run-all": "^4.1.5",
    "serve": "^14.2.3"
  }
}
